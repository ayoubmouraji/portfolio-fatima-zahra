# Portfolio FZ (Vite + React + Tailwind)

## Installation
```bash
cd fz-portfolio
npm install
```

## Lancement en local
```bash
npm run dev
```
Ouvre ensuite l'URL affichée (souvent http://localhost:5173).

## Build (optionnel)
```bash
npm run build
npm run preview
```
